package constants

const (
	// KongIngressClass is the string value needed by default to configure the annotation "kubernetes.io/ingress.class"
	// which indicates which ingress controller is responsible for an Ingress resource.
	KongIngressClass = "kong"
)
