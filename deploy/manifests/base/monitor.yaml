apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: ingress-kong-metrics
  namespace: kong
spec:
  selector:
    matchLabels:
      app: ingress-kong
  podMetricsEndpoints:
  - targetPort: 8100
    interval: 10s
  - targetPort: 10255
    interval: 10s