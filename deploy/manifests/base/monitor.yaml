---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: ingress-kong-metrics
  namespace: kong
spec:
  selector:
    matchLabels:
      app: ingress-kong
  podMetricsEndpoints:
  - targetPort: 8100
  - targetPort: 10255
