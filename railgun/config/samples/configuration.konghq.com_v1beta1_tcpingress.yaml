apiVersion: configuration.konghq.com.konghq.com/v1beta1
kind: TCPIngress
metadata:
  name: tcpingress-sample
spec:
  rules:
  - port: 9000
    backend:
      serviceName: tcp-echo
      servicePort: 2701```
