apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../kong-dbless
  - ../postgres
  - migration.yaml

patchesStrategicMerge:
  - kong-ingress-postgres.yaml

secretGenerator:
  - name: kong-settings
    namespace: kong
    behavior: merge
    envs:
      - kong.env