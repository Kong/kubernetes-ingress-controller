apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: openebs-cstor-pool-metrics
  namespace: openebs
spec:
  selector:
    matchLabels:
      app: cstor-pool
  podMetricsEndpoints:
  - targetPort: 8100
    interval: 10s
  - targetPort: 10255
    interval: 10s