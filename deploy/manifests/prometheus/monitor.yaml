---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ingress-kong-metrics
  namespace: kong
spec:
  selector:
    matchLabels:
      app: ingress-kong
  endpoints:
  - targetPort: 8100
    path: /metrics
  - targetPort: 10255
    path: /metrics
