apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: ingress-kong-metrics
  namespace: kong
spec:
  podMetricsEndpoints:
  - port: metrics
  - port: cmetrics
  selector:
    matchLabels:
      app: ingress-kong
